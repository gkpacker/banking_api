version: '3'

services:
  phoenix:
    build:
      context: .
      args:
        - SECRET_KEY_BASE=${SECRET_KEY_BASE}
        - HOST=${HOST}
        - PORT=${PORT}
    environment:
      PG_USER: postgres
      PG_PASSWORD: postgres
      POSTGRES_DATABASE: banking_api_prod
      POSTGRES_PORT: 5432
      DATABASE_URL: ecto://postgres:postgres@db/banking_api_prod
      MIX_ENV: prod
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: postgres:9.6
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 10s
      retries: 120
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
    restart: always
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
